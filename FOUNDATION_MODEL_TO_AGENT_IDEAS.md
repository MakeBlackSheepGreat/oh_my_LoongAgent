# 从基础模型效率到小模型 Agent 编排：研究备忘

**日期**：2026-07-30  
**用途**：将基础模型论文中的计算、上下文、验证和训练原则迁移到 12B 以下的多 Agent 系统。  
**证据状态**：已通读本地基础模型论文索引、对应 Idea 文件与摘要；正式写作前应再核验各论文的实验设置和原文结论。

## 1. 研究判断

基础模型团队面对的是“总参数、激活参数、KV cache、训练样本、测试时计算和服务成本如何共同分配”的问题。小模型 Agent 系统面对的是同构的系统问题：模型调用、上下文、检索、工具执行、验证、候选数和强模型升级如何共同分配。

因此，项目的重点可从“多个小 Agent 能否代替一个大模型”推进为：

> 在固定的显存、时延、API 额度和工具权限预算内，如何把有限的测试时计算动态投向最可能改善可验证任务结果的动作？

这里的动作包括检索技能、生成候选、运行工具、执行验证、修复、调用审查角色、升级云端模型和停止。模型选择只是其中一种动作。

## 2. 已核验来源与可迁移原则

| 基础模型来源 | 原始原则 | 可迁移到 Agent 系统的原则 | 需要避免的误读 |
| --- | --- | --- | --- |
| DeepSeekMoE, arXiv:2401.06066 | 细粒度路由专家加共享专家，使计算分配兼顾专门性与公共能力 | 建立系统级 MoE：共享的任务契约、技能库、仓库地图、工具 Schema 与验证器，加上按条件选择的角色技能 | 角色名增多不等于形成专家分工；必须证明路由稳定、角色信息互补 |
| DeepSeek-V2, arXiv:2405.04434 | MLA 压缩 KV cache，MoE 降低激活计算 | 将 Agent 历史视为可压缩状态缓存：保留带来源、版本和哈希的工件，向模型传递状态切片，按需回读原始证据 | 把完整聊天记录反复拼入上下文会放大 KV 与 token 成本 |
| DeepSeek-R1, arXiv:2501.12948；MiMo, arXiv:2505.07608 | 通过可验证信号和后训练提升推理 | 用测试、Schema、文件差分、约束检查和工具观察驱动候选修复；高价值轨迹再进入轻量后训练 | 同模型自评无法提供独立正确性，候选数量增加也会带来相关性错误 |
| DeepSeek-Prover-V1.5, arXiv:2408.08152 | 利用证明助手反馈与搜索 | 仅在验证器指出具体违反项时分支搜索，围绕任务状态和工件构建搜索树 | 将自然语言辩论直接扩展为树会造成指数级 token 增长 |
| MiniMax-M1, arXiv:2506.13585 | 提升测试时计算效率，并分别处理长上下文与推理预算 | 分开测量生成、检索、验证、工具等待和修复的成本；预算应投给预期收益最高的阶段 | 单看模型生成 token 或单次响应时延无法说明系统效率 |
| MiniMax-M2, arXiv:2605.26494 | Agent 任务轨迹、可执行工作空间、工件对齐奖励、训练推理解耦 | 建立 Agent-native 数据飞轮：每次任务沉淀可执行工件、验证结果和失败类型，筛选后编译为 skill、偏好样本或 LoRA 数据 | 用静态问答分数代替任务完成度会掩盖工具和状态失败 |
| MiniMax Sparse Attention, arXiv:2606.13392 | 只关注少量相关 KV block，实现稀疏长上下文 | 检索器对技能、历史工件、代码块和日志块做块级选择；模型上下文只保留被选块及可追溯引用 | “长上下文”本身不能保证相关证据被选中 |
| MaxProof, arXiv:2606.13473 | 生成、验证、修复、排序形成候选群体和淘汰赛 | 将候选扩展到“计划-工具动作-工件”路径，用硬验证优先、软评审次之的分层选择 | 固定生成十个答案再由同一模型投票，通常难以控制成本和相关性 |
| Kimi K2, arXiv:2507.20534 | 用真实环境的可执行结果定义 Agent 能力 | 以日常工具任务中的状态变化、约束满足和可回放轨迹作为主指标 | 通用聊天 benchmark 不能代表工具工作流可靠性 |

## 3. 候选核心创新：BVAR

建议将系统研究命名为 **BVAR：Budgeted Verifier-Routed Agentic Runtime**，中文可称“预算感知、验证器路由的 12B 以下 Agent 运行时”。

### 3.1 问题定义

给定总预算 `B = {显存, 时延, token, API 额度, 工具权限, 风险阈值}`，系统在每个状态 `s` 选择下一动作 `a`：

- 检索另一条技能或相似成功案例；
- 生成一个候选计划或补丁；
- 调用确定性工具或测试；
- 对明确错误进行修复；
- 选择审查角色；
- 向云端小模型池或强教师升级；
- 提前停止并交给人工。

路由目标是最大化“可验证的任务改进 / 资源成本”，而非最大化 Agent 数量或采样数量。

### 3.2 系统结构

```text
任务与预算
  -> 轻量状态分析器
  -> BVAR 调度器
       -> 共享能力层：技能库、工件库、工具 Schema、验证器、权限策略
       -> 条件角色层：检索 / 执行 / 修复 / 审查 / 升级
  -> 工具与环境
  -> 验证器阶梯
       -> 硬验证通过：提交工件
       -> 明确失败：定向修复或受限分支
       -> 不确定：追加候选、升级或人工审批
```

角色可由同一 8B/9B 模型串行复用，也可由不同云端 API 端点承担。系统级“专家”由提示、技能、允许工具、输入状态切片和输出 Schema 定义，无需在单卡上同时常驻多个模型。

### 3.3 与固定 Max 模式的实质差异

你提到的 MiMo Max 模式体现了测试时计算扩展的重要方向。产品具体是否固定为十次调用、候选是否独立、如何评估，仍需以官方技术说明核验。即使固定多次生成已存在，BVAR 的研究问题仍有清晰差异：

| 固定候选扩展 | BVAR |
| --- | --- |
| 预先指定候选数，例如 10 | 由验证器置信度、候选分歧、风险、剩余预算决定是否继续 |
| 主要比较最终文本 | 比较计划、工具参数、环境状态、工件与验证结果 |
| 候选常使用相近提示，错误高度相关 | 通过不同技能、不同检索证据、不同工具路径制造受控差异 |
| 成本随候选数线性增长 | 支持早停、定向修复、低成本验证优先和条件升级 |

创新点不能仅写成“把 10 次改为动态次数”。可成立的贡献是：构建可校准的收益估计器，使每一次新增计算都由独立验证信号和资源预算共同决定，并证明该策略给出更好的质量-成本前沿。

## 4. 三个可做且有区分度的子课题

### A. 系统级 MoE：共享能力与条件技能的最小分工

**问题**：全部角色都从零读取任务、文档和工具说明，导致上下文重复与协作噪声。

**方法**：共享能力层固定提供任务契约、规范化仓库地图、检索索引、工具 Schema、权限规则和硬验证器；条件角色只加载与当前失败类型匹配的 skill 包。路由器按任务类型、验证错误、历史成功率与预算选择技能。

**关键实验**：固定角色、随机路由、共享层加固定角色、BVAR 条件路由。报告角色间上下文重复率、工具错误率、成功率和成本。

### B. 验证器驱动的弹性测试时计算

**问题**：统一的多次采样会把计算投给容易任务，也可能在不可验证任务上无效扩张。

**方法**：首轮只生成 2 个有差异的候选。硬验证器判断是否有唯一通过结果；存在明确错误时让修复角色针对错误码工作；候选冲突、软验证不确定或任务高风险时，才增加候选或升级。通过早停控制总预算。

**可用信号**：测试覆盖率、Schema 合法率、约束违反数、候选间工件差异、检索证据一致性、工具错误码、剩余预算。

**关键实验**：单次、固定 `N=3`、固定 `N=10`、自我反思、BVAR。画出任务成功率与总 token、时延、API 成本的 Pareto 前沿。

### C. 经验编译器：将轨迹变成可执行技能

**问题**：原始 Agent 对话长、噪声大、难检索，个人算力无法依赖大规模重新训练。

**方法**：对成功轨迹抽取触发条件、最小上下文、工具序列、工件契约、验证器和失败修复；对失败轨迹记录首个错误点。生成 `skill_card`，并用回放测试确认它在保留任务上有效。只有通过回放的轨迹进入 Skill 库或 LoRA 候选集。

**关键实验**：无技能、全文轨迹 RAG、压缩 skill_card RAG、skill_card 加定向修复。报告上下文长度、检索精度、复用成功率、跨任务泛化与维护成本。

## 5. 云端免费或低成本 API 的角色

云端 API 让“单卡约束”从纯硬件约束变为预算与可复现性约束。建议组成可替换模型池：

- 本地 8B/9B：核心实验基线、私有数据、低风险执行、离线回放。
- 免费小模型 API：路由、抽取、候选多样化、离线轨迹构造和压力测试。
- 强教师 API：只处理高价值分歧点、难例标注和保留集参考。

每次调用写入 `provider`、`endpoint`、`model_id`、日期、请求配置、token、限流状态、成本和数据分类。对云端服务设置限额和熔断器；服务变更、免费额度或模型更新时，相关结果单独报告。敏感文件进入脱敏或本地模拟环境。

## 6. 最小实验包

1. 选定一个本地 8B/9B 模型和两个可替换云端小模型端点。
2. 建立 30-50 个具备自动验证条件的任务：文件组织、表格生成、日程约束、资料检索、受控代码修复。
3. 完成共享能力层：`task_contract_v1`、工件库、三类硬验证器、权限模拟器。
4. 首轮比较 B0 直接单 Agent、B1 技能 RAG、B2 固定多候选、B3 固定多 Agent、B4 BVAR。
5. 将 B4 的成功和失败轨迹编译为 skill_card，冻结保留集后做第二轮比较。

完成上述五步即可回答一个可发表且可复现的问题：在 12B 以下模型与混合 API 资源条件下，验证器驱动的动态计算分配是否优于固定多 Agent 和固定多采样。

## 7. 必须防止的伪创新

- 多加几个角色、改换提示或扩大上下文，缺少资源分配规则和消融实验。
- 只展示最佳案例，未报告固定预算、失败率和重复运行方差。
- 用同一个模型负责生成与最终裁决，未测量裁决错误。
- 使用云端服务却遗漏模型快照、版本、限流和费用信息。
- 将特定任务得分接近表述为通用前沿能力等价。

## 8. Kimi Attention Residuals 的 Agent 迁移：输入相关的历史选择

Kimi 的 `Attention Residuals`（arXiv:2603.15031）针对 PreNorm 残差以固定单位权重累积全部层输出而造成的隐藏状态膨胀与贡献稀释，改为按照当前输入对前序层表示进行注意力选择；其 `Block AttnRes` 用块级表示降低全历史注意力的内存和通信成本。

这给 Agent 的直接启发是：长程任务中的前序 Agent 输出、工具观察和中间摘要不应以固定权重累积进后续 prompt。每个当前角色都应按当前目标，从“过程残差”中选择少量相关且仍有效的历史状态。

### 8.1 候选模块：AAR

定义 **AAR：Agent Attention Residuals**。任务运行时维护原始工件库，并为每个阶段产生一条可追溯的残差信息：

```text
不可变任务约束
  + 阶段块摘要（研究 / 规划 / 执行 / 验证）
  + 细粒度残差（决策、工具观察、diff、测试、未决约束）
  -> 当前角色目标查询
  -> 约束门控 + 块级选择 + 细粒度展开
  -> 最小工作上下文
```

每条残差带有类型、来源 URI、版本、依赖、状态（有效 / 已替代 / 未决 / 已验证）、摘要和原始工件引用。不可变任务约束与权限策略始终注入；其他历史信息按相关性、依赖性、时效性、验证状态和 token 成本选择。

### 8.2 与普通 RAG 的区分

普通 RAG 主要面向外部文档检索。AAR 处理的是系统自身在执行过程中留下的决策、状态变化和验证反馈，并维护“哪个输出替代了哪个输出”“当前结果依赖哪些证据”的因果关系。它适合解决长程 Agent 中的上下文稀释、过期计划复用、测试反馈丢失和多角色重复阅读问题。

### 8.3 Block AttnRes 对应的工程近似

把连续轨迹划为阶段块，每个块只保留紧凑摘要、关键约束和工件索引。当前任务先对块打分，选中的块再展开细粒度残差；嵌入检索、规则依赖和轻量重排序器均可实现这一过程，无需修改基础模型注意力层。该模块可作为 BVAR 中“读取历史残差”的低成本动作。

### 8.4 证伪实验

在多阶段文件整理、日程冲突修复、检索报告修订和受控代码修复任务中比较：全量历史、最后摘要、外部文档 RAG、固定阶段摘要、AAR。除成功率和 token 外，增加测量过期状态引用率、约束遗漏率、工件溯源完整率、重复读取率和失败恢复率。AAR 只有在这些指标与质量-成本前沿上表现出稳定收益时，才值得保留为系统核心能力。
