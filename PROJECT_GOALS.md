# 项目目标

**版本**：v0.3  
**更新时间**：2026-07-31  
**项目名称**：面向 8B 参数大模型设计的日常多 Agent 系统

本文件只定义项目目标、研究问题、成功指标和最终成果。执行计划见 `SPEC.md`，硬边界见 `RULES.md`，主观风格见 `SOUL.md`，候选创新见 `IDEA.md`。

## 总目标

构建并评测一个以本地 8B/9B 模型为核心、可选择低成本云端模型的通用 Agent Harness。系统以任务契约、受限工具、版本化工件、验证器、恢复、审计、Skill 和记忆为底座，在有限显存、token、时延和 API 预算内完成真实工具任务，并保持执行可验证、状态可回放、错误可恢复、风险可治理。

项目需要回答一个中心问题：良好的 Harness、结构化通信、工件记忆、验证反馈和动态计算分配，能够将 12B 以下模型的任务级能力推进到什么程度，代价与边界分别在哪里？

## 目标用户与场景

目标用户是只有单卡或有限 API 预算、仍希望构建可靠智能工作流的个人开发者、学生与小型团队。

首期场景覆盖：

- 本地资料检索、摘要、结构化抽取和引用核验；
- 文件整理、表格生成、日程约束与任务规划；
- 受控代码定位、局部修复、测试和结果审查；
- 模拟邮件、工单、日历和高风险审批工作流。

## 研究问题

| ID | 问题 | 主要对照 | 需要得到的答案 |
| --- | --- | --- | --- |
| RQ1 | Harness 能为固定 8B 模型带来多少任务增益？ | 简单工具循环、工程闭环 | 工具、状态、验证与恢复各自贡献多少 |
| RQ2 | 哪些任务值得触发多 Agent 协作？ | 单 Agent、固定角色、动态触发 | 协作增益何时超过通信和时延成本 |
| RQ3 | 如何在固定预算内分配测试时计算？ | 单次、固定候选、固定多 Agent、动态路由 | 哪种策略形成更优质量-成本前沿 |
| RQ4 | 外部技能与记忆能补偿多少模型能力？ | 直接提示、长上下文、检索式 Skill | 补偿区间、饱和点和噪声边界在哪里 |
| RQ5 | 强教师经验如何迁移到小模型系统？ | 最终答案、整段轨迹、首个分歧点、经验编译 | 哪类可观察反馈具有最高样本效率 |
| RQ6 | 系统如何在风险约束下稳定执行？ | 无治理、Schema、权限、审批和审计 | 越权、泄露、误操作与人工接管如何变化 |

## 产品能力目标

## 架构边界

- 通用核心只处理任务、动作、工件、状态、预算、权限、验证、策略和回放，不依赖论文、PDF、DOI、arXiv 或任何单一垂直领域。
- 论文搜索是首个正式 `Skill Pack`、首个发布场景和首个评测场景；文件整理以模拟 Skill 作为核心回归样例。后续代码维护、表格和日程仅替换 Skill、工具和验证器。
- `BVAR` 是 B4 条件下的预算感知验证器路由策略，而非框架边界；它必须与 B0-B3 共享运行时、预算和审计接口。
- `GPT-5.6-sol` 等强教师仅用于离线生成和抽检冻结参考包。用户运行时只使用配置的学生模型、受控工具与人工审批。

### G1：可执行

系统能够理解任务、生成类型化动作、调用受限工具并提交满足验收条件的工件。

### G2：可验证

每个任务拥有明确成功判据；工具结果、状态差分、引用、测试和权限均可由独立验证器检查。

### G3：可回放

任务输入、模型配置、动作契约、工具观察、工件版本、验证结果和终态可以重建一次执行。

### G4：可恢复

失败能够归类到任务理解、状态读取、路由、工具、参数、验证、权限或运行时，并导向定向修复、升级、停止或人工审批。

### G5：预算可控

模型调用、上下文、候选、工具等待、并发、强模型升级和总成本拥有显式预算与停止条件。

### G6：经验可积累

经验证的成功与失败可以转化为可检索技能、测试夹具、错误映射和可选轻量训练数据，并在保留集上检验复用价值。

## 成功指标

| 维度 | 指标 |
| --- | --- |
| 任务质量 | 成功率、约束满足率、证据正确率、Schema 合法率 |
| 工具可靠性 | 工具选择、参数正确率、执行成功率、回滚成功率 |
| 协作效率 | 端到端时延、token、显存峰值、调用次数、无效通信、重试率 |
| 恢复能力 | 失败定位率、恢复成功率、错误复现率、过期状态引用率 |
| 治理 | 越权拦截率、泄露检测率、人工接管率、审计完整率 |
| 可复现性 | 重复运行方差、环境记录完整度、工件哈希与回放成功率 |

## 最终成果

1. 一套可在单卡环境运行的 8B/9B 日常 Agent 运行时。
2. 一个包含模拟工具、确定性验证器和风险分层的中文日常任务集。
3. 单 Agent、固定多 Agent、动态路由和教师反馈系统的完整消融结果。
4. 可复用的动作契约、工件协议、审计事件、错误码和失败分类规范。
5. 可公开的代码、匿名化任务数据、实验报告、数据卡、模型卡和复现指南。

## 项目完成定义

核心成果能够在独立环境复跑；主要结论有固定预算下的直接基线、重复实验与失败案例支持；用户可以完成至少一个端到端日常工具工作流，并清楚看到工件、验证、成本、风险与恢复状态。
