# 项目候选创新与实验方向

本文件只保存候选创新点、创新方向、可检验假设与实验思路。项目要求、代码口味、硬边界、任务计划、进度记录和已采纳决策分别由 `SOUL.md`、`RULES.md`、`SPEC.md`、`specs/` 与 `guidance/DECISIONS.md` 管理。

所有条目在完成直接文献比较、可复现基线和保留集实验前，均保持“候选”状态。

## I-001：小模型基础执行层与条件升级

**提出时间**：2026-07-30  
**状态**：候选

**创新方向**：让 8B/9B 模型承担边界清楚、高频、可验证、可撤销的节点，如分类、抽取、检索改写、摘要、工具路由和格式修复；复杂规划、高不确定性和高风险任务按条件升级给云端模型、强教师或人工。

**依据**：中小模型分层协作研究、10B 以下 Agent 部署研究以及小模型主动求助工作共同支持能力分层；现有方案对日常工具任务中的升级触发、成本和恢复收益覆盖有限。

**实验方向**：为任务标注复杂度、风险、可验证性和工具依赖，比较全本地、固定阈值升级、学习式升级与强模型全覆盖。报告成功率、升级率、时延、成本和错误类型。

## I-002：动态协作触发器

**提出时间**：2026-07-30  
**状态**：候选

**创新方向**：将多 Agent 协作视为有成本的运行时动作。只有子目标可独立验收、单 Agent 不确定性升高、工具失败需要隔离诊断、风险需要独立审查或任务适合并行时触发协作。

**依据**：强单 Agent 与多 Agent 的比较研究表明协作收益受任务结构影响；不确定性路由和自适应拓扑工作提供了触发信号。

**实验方向**：对比单 Agent、固定角色、规则触发、学习式触发和熵驱动触发；固定模型、工具与总预算，测量净成功率、无效通信、端到端时延和 token。

## I-003：任务契约与工件引用通信

**提出时间**：2026-07-30  
**状态**：候选

**创新方向**：设计 `task_contract_v1`，让 Agent 交换任务 ID、目标、输入工件、输出 Schema、状态增量、证据、权限、错误码和下一动作。自然语言协商只在语义冲突时按需发生。

**依据**：多 Agent 通信研究指出长文本转述会增加上下文膨胀、字段歧义、隐私泄露、迎合和共识偏差；A2A、MCP 与相关 ACP 协议提供互操作基础。

**实验方向**：与全量聊天、阶段摘要和共享黑板对比，测量 token、Schema 合法率、状态一致性、错误复现率、隐私泄露检测率和恢复成功率。

## I-004：控制面、数据面与状态切片

**提出时间**：2026-07-30  
**状态**：候选

**创新方向**：控制面负责路由、状态机、重试、超时、审批和审计；数据面负责文档、代码、表格、证据与中间工件。当前角色只读取满足任务依赖的最小状态切片。

**依据**：Agentic Search、自适应通信拓扑、DeepSeek-V2 的上下文压缩和稀疏注意力研究都提示，状态选择的质量可能比完整历史长度更关键。

**实验方向**：对比全量历史、最后摘要、普通向量检索和依赖感知状态切片，记录输入 token、重复读取率、过期状态引用率、约束遗漏率和任务成功率。

## I-005：中文日常工具任务分层基准

**提出时间**：2026-07-30  
**状态**：候选

**创新方向**：建立覆盖信息处理、规划协同、可撤销工具操作和模拟高风险审批的中文日常任务集。每个任务包含初始状态、允许工具、风险级别、硬验收条件和人工参考。

**依据**：现有个人助手、GUI、软件工程和垂直工作流基准提供局部先例；中文、本地文件、日程、表格和混合工具调用的统一小模型多 Agent 基准仍有研究空间。

**实验方向**：分别报告短任务与长任务、单工具与多工具、低风险与高风险子集，检查任务难度、验证器误差、模型排名稳定性和人工评分一致性。

## I-006：强教师首个分歧点蒸馏

**提出时间**：2026-07-30  
**状态**：候选

**创新方向**：让强教师 Agent 与 8B 学生系统运行同一任务，比较显式计划、工具参数、观察、状态变化、工件和验证结果，定位最早产生实质后果的分歧点。将该分歧编译为路由规则、技能、偏好对或轻量后训练样本。

**依据**：最早错误点修正、多教师在线蒸馏、协作轨迹蒸馏、MCP 模块蒸馏和求助策略均有直接先例；多小模型系统围绕可观察分歧持续进化的完整闭环仍值得验证。

**实验方向**：比较整段轨迹模仿、最终答案蒸馏、首个分歧点修正和错误簇课程学习，在冻结保留集上测量迁移收益、样本效率、遗忘和教师调用成本。

## I-007：模型能力与 Harness 深度的二因素实验

**提出时间**：2026-07-30  
**状态**：候选

**创新方向**：将基础模型能力和工程 Harness 深度作为两个独立实验轴，量化结构化工具、受限工作区、状态工件、测试反馈、恢复策略和独立审查的增益。

**依据**：SWE-agent、Agentless 以及长程 Agent Harness 研究均表明运行时接口显著影响任务完成；8B 模型在不同 Harness 深度下的收益上限和失效模式仍缺少统一实证。

**实验方向**：模型轴采用固定 8B 与强教师；Harness 轴采用单轮调用、单 Agent 闭环、任务契约加审查器、教师反馈迭代。测量工具正确率、测试通过率、恢复率、成本和模型-Harness 交互效应。

## I-008：外部空间对小模型能力的补偿曲线

**提出时间**：2026-07-30  
**状态**：候选

**创新方向**：把文档、代码示例、测试夹具、工具说明和历史经验存入持久空间；通过检索和重排序形成小型工作上下文。研究外部空间在何种任务与规模下能够补偿小模型能力，以及何时开始产生检索噪声。

**依据**：代码 FIM、项目级代码语料、Skill、长上下文与稀疏检索研究表明高质量局部上下文能够显著改变小模型表现。

**实验方向**：比较直接提示、固定长上下文、Skill 检索、Skill 加相似变更、Skill 加确定性测试和有限教师升级。绘制任务成功率相对存储量、检索 token、时延和维护成本的曲线。

## I-009：BVAR 预算感知验证器路由

**提出时间**：2026-07-30  
**状态**：重点候选

**创新方向**：构建 `BVAR`（Budgeted Verifier-Routed Agentic Runtime）。系统在每个任务状态下，根据验证器置信度、候选差异、风险和剩余预算，在检索、生成、工具执行、验证、修复、审查、模型升级与停止之间选择下一动作。

**依据**：Mooncake 的 SLO 调度与早拒绝、MiMo 的难度驱动计算、DeepSeek-R1/Prover 的验证反馈、MiniMax-M1 的测试时计算和 MaxProof 的生成-验证-修复共同提供机制来源。

**实验方向**：与单次生成、固定 `N=3`、固定 `N=10`、固定多 Agent 和自我反思比较，绘制成功率、token、时延、显存与 API 成本的 Pareto 前沿，并校准每类动作的预期增益估计。

## I-010：AAR 过程残差记忆

**提出时间**：2026-07-30  
**状态**：重点候选

**创新方向**：构建 `AAR`（Agent Attention Residuals）。系统把任务约束、决策、工具观察、工件版本、测试结果和未决问题保存为过程残差，按当前目标选择阶段块，再展开少量有效细节。

**依据**：Kimi Attention Residuals 的输入相关历史选择、Kimi Linear 的有限状态记忆、DeepSeek-V2/V4 和 MiniMax Sparse Attention 的稀疏上下文机制提供设计来源。

**实验方向**：对比全量历史、滑动窗口、最后摘要、普通 RAG、固定阶段摘要和 AAR；报告约束遗漏、过期状态引用、工件溯源、重复读取、失败恢复和 token。

## I-011：AGI 最小工件缺口填补

**提出时间**：2026-07-30  
**状态**：重点候选

**创新方向**：构建 `AGI`（Artifact Gap Infilling），将当前任务表示为已验证工件中的一个最小类型化缺口，例如函数补丁、表格单元、日程冲突、缺失引用或工具参数。每次模型动作只改变一个可验证槽位。

**依据**：GLM 的 blank infilling、DeepSeek-Coder 的 FIM、项目级代码补全和 GLM-OCR 的结构化输出表明局部缺口可以形成清晰生成边界。

**实验方向**：与整份工件重写、自由编辑和文件级补丁比较，测量修改范围、验证通过率、回归错误、修复轮数和 token。需额外检查复杂任务被过度碎片化时的协调成本。

## I-012：HCA 局部执行与全局审计

**提出时间**：2026-07-30  
**状态**：候选

**创新方向**：构建 `HCA`（Hybrid Context Agent）。日常步骤使用局部工作区推进；验证失败、跨阶段依赖、风险升高或固定检查点出现时，触发全局工件审计。

**依据**：MiMo-V2-Flash、Kimi Linear、DeepSeek-V4 的局部-全局混合注意力以及多时间尺度建模工作提供启发。

**实验方向**：比较全程全局上下文、固定周期审计、事件触发审计和学习式审计，测量全局读取次数、遗漏依赖、延迟、token 和长程任务成功率。

## I-013：DRA 双表示动作编译

**提出时间**：2026-07-30  
**状态**：重点候选

**创新方向**：构建 `DRA`（Dual-Representation Action）。用户请求先形成保留语义细节的意图表示，再编译成带权限、前置条件、输入输出 Schema、验收器和回滚策略的动作契约。

**依据**：Janus 的理解与生成解耦、WebGLM 的检索生成评分分工、GLM All Tools 和 GLM-5V-Turbo 的工具与 Skill 机制支持表示分离。

**实验方向**：对比自由文本工具调用、单层 JSON 动作和 DRA，测量意图保持率、参数正确率、越权拦截、歧义恢复和执行成功率。

## I-014：E-Compiler 经验编译器

**提出时间**：2026-07-30  
**状态**：重点候选

**创新方向**：构建 `E-Compiler`，把经验证的成功轨迹、失败修复和教师反馈压缩为 `skill_card`、测试夹具、错误码映射、偏好对及可选 LoRA 样本。技能携带触发条件、最小上下文、工具序列、工件契约和失效条件。

**依据**：Kimi-Dev 的可验证技能先验、Kimi k1.5 的 long-to-short、DeepSeek-Prover 的可验证数据和 MiniMax-M2 的 Agent 原生经验提供直接来源。

**实验方向**：比较原始轨迹 RAG、摘要 RAG、`skill_card`、带回放筛选的 `skill_card` 和轻量后训练；测量检索精度、上下文长度、复用成功率、跨任务迁移、遗忘和维护成本。

## I-015：验证器阶梯与元验证

**提出时间**：2026-07-30  
**状态**：候选

**创新方向**：按成本与确定性构造验证器阶梯：Schema、权限、静态规则、状态差分、测试、证据一致性、模型审查和人工审批逐级启用。额外评估验证器自身的误报、漏报和偏差。

**依据**：DeepSeekMath-V2 的生成器-验证器-元验证、DeepSeek Prover 系列的形式反馈、MaxProof 的候选淘汰以及工具 Agent 的执行结果评测共同支持分层验证。

**实验方向**：比较单模型自评、单硬验证器、固定验证器链和预算感知阶梯，测量最终错误率、验证成本、误报、漏报、人工接管与候选淘汰效率。

## I-016：A2A、MCP 与控制协议的分层组合

**提出时间**：2026-07-30  
**状态**：候选

**创新方向**：内部任务状态使用 `task_contract_v1`，跨 Agent 委派使用 A2A，工具与数据连接使用 MCP，高风险动作增加身份、授权、委派链和审计控制。Agent Client Protocol 只处理未来 IDE 接入。

**依据**：Agent Communication Protocol、Agent Client Protocol 和 Agent Control Protocol 存在同名异义；互操作协议综述显示消息、资源与治理需要不同层次的契约。

**实验方向**：实现最小适配层，比较内部直连、标准协议和多协议组合的开发成本、消息开销、错误可定位性、权限表达与跨框架迁移成本。

## I-017：六模块统一运行时

**提出时间**：2026-07-30  
**状态**：综合候选

**创新方向**：形成以 BVAR 为调度核心的组合：`DRA` 编译动作，`AAR` 选择过程状态，`AGI` 定位工件缺口，验证器提供反馈，`HCA` 管理局部与全局节奏，`E-Compiler` 沉淀经验。

**依据**：59 篇 DeepSeek、GLM、Kimi、MiMo 与 MiniMax 基础模型论文反复出现条件计算、上下文压缩、局部-全局混合、测试时计算、可验证反馈和经验蒸馏六类规律。逐篇映射见 `FOUNDATION_MODEL_AGENT_TRANSFER_MATRIX.md`。

**实验方向**：采用递增消融验证各模块的独立与交互收益：基础单 Agent、DRA、DRA+AAR、加入 AGI 与验证器、加入 BVAR、加入 HCA、加入 E-Compiler。统一报告任务质量、成本、恢复、治理指标和模块维护开销。

## I-018：Quant-Harness 执行机制组

**提出时间**：2026-08-01  
**状态**：综合候选

**创新方向**：借鉴量化交易执行系统的"零冗余、高可靠"设计，形成一组可独立验证的 Harness 执行机制：
1. **KV 前缀缓存与长上下文压缩**：重复上下文不重算；长对话按关键点自动摘要并动态调整窗口。
2. **向量存储与会话持久化**：记住项目架构与编码习惯，跨会话无缝续接，形成长期记忆。
3. **工具链式调用与错误回退**：工具失败按 重试→降级→切换备用方案 的顺序自愈，失败留下结构化证据。
4. **主从多 Agent 通信协议**：主 Agent 拆任务→分发子 Agent（独立提示词与权限）→聚合结果。
5. **任务规划图自优化**：模型输出 DAG 执行路径，框架自动优化执行顺序。
6. **动态工具注册**：无需重启即可注册新工具，模型与 Harness 共同进化。

**依据**：2026-07 下旬多家公开媒体对 DeepSeek Harness 内部测试招募、团队组建与负责人（浙江大学计算机系，前 Jane Street 量化交易系统工程师约 9 年）背景的报道（见 ATTRIBUTIONS.md R-001）。六模块产品形态来自媒体报道中对团队招聘方向的推断，未经官方确认，仅作设计参考；"量化执行系统思维"方向与负责人职业背景一致。各机制与本项目既有候选重叠：1 对应 I-004/I-010，2 对应 I-008，3 对应 D-004 恢复策略，4 对应 I-002/I-003，5 为新的 DAG 自优化方向，6 对应 I-016 与 C-007 的插件式工具注册。

**实验方向**：在 B0-B4 基线框架内逐项加入并消融：前缀缓存与动态窗口摘要、持久记忆、链式回退、主从分发、DAG 执行顺序优化、运行期工具注册。统一报告任务成功率、token、时延、恢复率、无效通信、注册与重启成本。
