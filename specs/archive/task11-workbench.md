# task11：项目中心 Agent 工作台

**状态**：retired  

> **迁移说明（D-014，2026-08-01）**：本任务的 Python/React 实现已删除，不做存档。相关能力以 Go 在 task14 对应子任务中重新实现。下方验证证据为历史记录，仅供追溯。

**目标**：建立以多本地项目与用户-Agent 对话为中心的三栏工作台。聊天先产出可审阅任务草案，审批后进入通用 Harness 运行时；论文搜索仅作为首个可调用 Skill 与验证场景。

| ID | 内容 | 状态 | 验收条件 |
| --- | --- | --- | --- |
| task11.0 | 冻结工作台边界与数据契约 | completed | 本 Spec、主计划与决策记录描述项目、会话、草案、插件和运行边界 |
| task11.1 | Workbench 持久化模型 | completed | `workbench.sqlite3` 保存项目、会话、消息、草案、插件配置、模型偏好和工作项 |
| task11.2 | 可信项目根目录 | completed | 多根目录注册、树读取、UTF-8 上下文上限与路径逃逸拦截经测试 |
| task11.3 | 会话与 SSE | completed | 会话消息、可见答复、事件序号与 `after` 断点续连接口可用 |
| task11.4 | 聊天草案编排 | completed | Provider JSON 编排与离线受限规划器仅产出可见答复和结构化草案 |
| task11.5 | 草案审批与运行关联 | completed | 仅获批草案创建运行，任务元数据保存项目工作区与会话来源 |
| task11.6 | 本地 Skill 插件 | completed | 已注册 Skill 可按项目启停，配置接口包含默认预算 |
| task11.7 | 左侧项目导航 | completed | 项目切换、文件树、新建任务、PR、已安排、插件和底部控制可用 |
| task11.8 | 中央聊天体验 | completed | 消息、上下文文件、草案提示和运行状态连续展示 |
| task11.9 | 右侧检查器 | completed | 计划、输出、Agent、来源、上下文五个同步面板可切换 |
| task11.10 | Skill 与运行观察接入 | completed | `literature_search`、`file_organizer`、工件、验证、导出和运行事件可由会话观察 |
| task11.11 | PR 与已安排视图 | completed | 本地工作项表、导航和空状态可用，外部 Git 与调度适配器留待后续 |
| task11.12 | 响应式与可访问性 | completed | 桌面三栏与 390px 窄屏抽屉经浏览器检查；关键按钮与表单具备语义标签 |
| task11.13 | 启动、测试与发布记录 | completed | 动态端口、README、40 个后端测试、TypeScript 构建和浏览器验证完成 |

**依赖**：用户已明确要求提前实施本任务；实现未改变 task8 的 B0-B3 基线验收范围。

**数据边界**：项目根目录须由用户显式信任；客户端只提交逻辑文件选择，服务端完成路径解析、大小检查和工作区限制。模型密钥只由环境变量提供。持久化内容仅包含用户可见消息、任务草案、动作、工具观察、工件、验证与错误。

**验证证据**：`pytest -q` 通过 40 项测试；`npm run build` 完成 TypeScript 与 Vite 构建；浏览器在 `127.0.0.1:48639` 验证项目注册、聊天生成论文搜索草案、桌面三栏、上下文参数面板和 390px 抽屉布局。后端 HTTP 契约测试覆盖项目、会话、草案审批和运行关联。
